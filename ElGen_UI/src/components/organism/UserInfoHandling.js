const UserInfoHandling = (oldUserTag) => {
    // Обработка oldUserTag
    const processedTag = oldUserTag;

    // Возврат обработанного значения
    return processedTag;
}

export default UserInfoHandling;